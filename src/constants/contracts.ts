// import { getContract } from "viem";

/*
 * This file contains all the contract instances
 * This uses the new viem library. Need to slowly replace all ethers.js code with viem
 */
// export const bridgeEntranceContract = getContract({
//   address:
// })

export const bridgeEntranceContract = "0x0";
